/**
 * Numeric guard-rails for the percentage engine — interface convenience,
 * not a policy. Every calculation still receives explicit numbers.
 */

/** Largest magnitude the engine will accept for any input field. */
export const MAX_MAGNITUDE = 1_000_000_000_000;
